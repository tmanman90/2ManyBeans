import { C, radius, shadows, type as typeScale } from '../../../styles/theme';
export function DataGapCard({ title = 'Tell Professor Ruphus about the cup', options = [], onSelect }) { return <div data-artifact="data_gap" style={{ padding: 14, border: `1px solid ${C.hairline}`, borderRadius: radius.lg, boxShadow: shadows.e1, background: C.cream }}><div style={typeScale.h3}>{title}</div><div style={{ display: 'grid', gap: 8, marginTop: 10 }}>{options.map((option) => <button key={option.id || option.value || option} type="button" onClick={() => onSelect?.(option)} style={{ minHeight: 44, textAlign: 'left', padding: '8px 12px', border: `1px solid ${C.hairline}`, borderRadius: radius.md, background: 'transparent', color: C.text }}>{option.label || option.value || option}</button>)}</div></div>; }
