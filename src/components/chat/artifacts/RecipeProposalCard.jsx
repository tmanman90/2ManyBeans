import { C, radius, shadows, type as typeScale } from '../../../styles/theme';
import { ArtifactAction } from './ArtifactAction';
export function RecipeProposalCard({ proposal = {} }) { const before = proposal.before || {}; const after = proposal.after || {}; const rows = [['Ratio', before.ratio, after.ratio], ['Temperature', before.waterTemp?.celsius, after.waterTemp?.celsius], ['Grind', before.grindSize?.setting, after.grindSize?.setting], ['Dose', before.coffeeGrams, after.coffeeGrams]]; const status = proposal.status || 'proposed'; return <div data-artifact="recipe_proposal" data-status={status} style={{ width: '100%', padding: 14, border: `1px solid ${C.hairline}`, borderRadius: radius.lg, boxShadow: shadows.e1, background: C.cream }}><div style={typeScale.h3}>One change</div>{rows.filter(([, oldValue, newValue]) => oldValue != null || newValue != null).map(([label, oldValue, newValue]) => <div key={label} style={{ display: 'flex', justifyContent: 'space-between', gap: 12, paddingTop: 7, fontVariantNumeric: 'tabular-nums' }}><span style={{ color: oldValue === newValue ? C.textMuted : C.text }}>{label}</span><span style={{ color: oldValue === newValue ? C.textMuted : C.text }}>{oldValue === newValue ? String(newValue) : `${oldValue} → ${newValue}`}</span></div>)}<details style={{ marginTop: 10 }}><summary>All controls</summary><pre style={{ whiteSpace: 'pre-wrap', color: C.textMuted }}>{JSON.stringify(after, null, 2)}</pre></details><div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 12, minHeight: 44 }}><ArtifactAction action="apply_proposal" label="Available soon" status="unavailable" /><ArtifactAction action="brew_once" label="Available soon" status="unavailable" /><button type="button" disabled style={{ minHeight: 44, border: 0, background: 'transparent', color: C.textMuted }}>Available soon</button></div></div>; }
