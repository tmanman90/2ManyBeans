# Loop Contract — brew-algo-fixes

Generated by `/loop-prompt` from a deepened plan. This is the **completion
contract** the autonomous run grades against. Drive it with `/goal` (or follow
it in-session). Do not re-plan — the plan is the source, this is the gate.

- Source plan: `docs/plans/2026-07-19-002-brew-algo-fixes.md`
- Executor: `goal` (isolation: `branch`)
- Resolved spec: `loop.resolved.json`

## Model Economy (enforced every iteration)

- **Orchestrator + code authoring:** `fable` (main loop keeps judgment + code).
- **Sub-agents (mechanical / read-heavy):** `sonnet` — sim build-run/tap/screenshot, exploration, ce-debug, broad reads, report writing.
- **Reviewer:** `codex`, cadence `per_phase` — adversarial review only, never implementation (zero Claude tokens).

## Goal

Fix every defect and evidence-backed miscalibration the 2026-07-19 brew algorithm audit found, across the hand-brew recipe engine (dead family temp bands, wrong troubleshooting directions, unfaithful Kasuya, dark/natural family values) and the Aiden prompt rules (curve mandate, over-broad washed/Kenya enforcement, unsupported age deltas), with the census-calibrated numbers, while leaving Tal's dialed-in Aiden grind path and all saved recipes byte-for-byte untouched.

## Definition of Done

R1-R15 implemented and proven by the verify harnesses; R16 pin green (Aiden grind tables byte-identical to main@1abc587); the real-pipeline A/B diff report committed showing exactly what changes for newly generated recipes and that saved shapes are untouched (R17); build and all regression suites green; dev-channel ship live with a -devapp timestamped bundle (R18); prod untouched.

## Verification Criteria (the independent gates)

- `v1-grind-pin` (programmatic): run `["node", "scripts/verify-grind-calibration.mjs"]`, expect `exit_zero`
- `v2-brew-params` (programmatic): run `["node", "scripts/verify-brew-params.mjs"]`, expect `exit_zero`
- `v3-build` (programmatic): run `["npm", "run", "build"]`, expect `exit_zero`
- `v4-regressions` (programmatic): run `["bash", "-c", "for t in scripts/*regression*.test.mjs; do node \"$t\" || exit 1; done"]`, expect `exit_zero`
- `v5-ab-compare` (programmatic): run `["node", "scripts/aiden-ab-compare.mjs", "--assert-invariants"]`, expect `exit_zero`
- `v6-codex-review` (judge via codex): Adversarially verify per phase: (a) every changed value matches the audit evidence exactly (dark 1:15.5-16.5 + 88-91C, natural 1:15.5-16.5 hot, Kasuya 93C/30s/4-5 pour, FP [13,17], census-backed Aiden softenings); (b) scope guard intact — FAMILY_GRIND_BANDS, enforceDeterministicGrind, ODE_GEN2_STEPS, GRINDER_MICRON_SCALES, classifyFamilyFallback, persistence paths, UI components all byte-unchanged; (c) prompt edits are anchored exact-string patches, no drift, no accidental mandate inversions (flat must NOT become a new mandate — the rule is match-reference-shape); (d) worst case to hunt: a P2 edit that silently changes deterministic enforcement output for a bean family Tal brews (Kenya washed, washed floral, clean natural). Known-good context: Liquid Glass gradients are intentional material; ambient loops exempt from the 300ms motion rule.
- `v7-shipdev` (programmatic): run `["bash", "-c", "npx @capgo/cli@latest channel currentBundle dev com.talmeltzer.coffeehub.dev | grep -q devapp"]`, expect `exit_zero`

## Evidence Map (every requirement -> proof)

- `R1` -> `v2-brew-params`
- `R10` -> `v6-codex-review`
- `R11` -> `v2-brew-params`
- `R12` -> `v2-brew-params`
- `R13` -> `v2-brew-params`
- `R14` -> `v2-brew-params`
- `R15` -> `v2-brew-params`
- `R16` -> `v1-grind-pin`
- `R17` -> `v5-ab-compare`
- `R18` -> `v7-shipdev`
- `R2` -> `v2-brew-params`
- `R3` -> `v2-brew-params`
- `R4` -> `v2-brew-params`
- `R5` -> `v2-brew-params`
- `R6` -> `v2-brew-params`
- `R7` -> `v2-brew-params`
- `R8` -> `v2-brew-params`
- `R9` -> `v2-brew-params`

## Gates

### plan_gate

- Policy: `revise_until_clean`
- Verdict source: `codex`
- Criteria: `v6-codex-review`
- Max revisions: `2`

### delivery_gate

- Policy: `revise_until_clean`
- Verdict source: `codex`
- Criteria: `v1-grind-pin, v2-brew-params, v3-build, v4-regressions, v6-codex-review`
- Max revisions: `3`

## Council

- `codex`: judge (cli: `codex`)

## Loop Control (brakes)

- Max iterations (units): `7`
- Cycle cap per unit/phase: `4`  (CLAUDE.md: stop on 5th attempt)
- Quality streak to ship: `2` consecutive clean runs
- No-progress stop: same failure x`2` -> `stop`
- Budget ceiling: `tokens=12000000`

## Scope Guard (do NOT touch)

- NEVER modify FAMILY_GRIND_BANDS values, enforceDeterministicGrind, ODE_GEN2_STEPS/nearestOdeStep, or GRINDER_MICRON_SCALES
- NEVER touch saved-recipe persistence, Firestore schemas, useAppData/useAidenBrew write paths
- NO changes to classifyFamilyFallback, gemini.js, professorRuphus.js, tasting wizard, onboarding, UI components
- NO production deploy; dev channel only; never bump package version for dev
- NO pushToAiden calls or anything touching the physical brewer
- Harness-first ordering — verify-brew-params.mjs (U2) lands before the fixes it gates where feasible

## Stop & Surface (from CLAUDE.md verifier loop)

- Hit the cycle cap -> stop, surface current state. Do not start another attempt.
- Same failure twice -> stop. That's guessing, not fixing.
- Never weaken a gate to pass (loosening a band, skipping a test, pinning a constant). Report it.
- Budget ceiling reached -> stop and surface what's green and what's left.
- Completion is refused unless every requirement in the Evidence Map maps to named, passing evidence.

## Context Sources

- Read `docs/plans/2026-07-19-002-brew-algo-fixes.md`
- Read `docs/data/algo-audit-2026-07-19/REPORT.md`
- Read `docs/data/algo-audit-2026-07-19/census-addendum.md`
- Read `docs/data/algo-audit-2026-07-19/fellow-official-drops-addendum.md`
- Read `docs/data/algo-audit-2026-07-19/aiden-legacy-vs-proposed.md`
- Read `lessons.md`

## Start

If running now: read the source plan + context, then drive the units through the
gates above until the Definition of Done is met with full evidence, a brake trips,
or the budget ceiling is hit. Then post a single report.
