# onboarding-100x loop log (orchestrator decisions)

- D1 (U2): `:` added to palateSummaryLine sanitize charset to match the spec's example format. APPROVED.
- D2 (U2): TastingWizard/RotationTab lacked profile access; orchestrator wired `onboardingPalate` prop App→RotationTab (getRecommendations 3rd arg) and App→TastingTab→TastingWizard (applyPalateTiebreak around predict). Defaults null; existing users identical.
- D3 (U9): R08 "Not now" uses the shared OnboardingCtaBar secondary slot (14px) instead of the spec's 15px one-off. APPROVED: shared-primitive consistency beats a 1px literal.
- D4 (U10): posters generated for celebrating + writing-notes (current R09/R10 videos) so reduced-motion is never broken pre-Phase-2; 19/19 videos now have posters.
- D5 (spec): R10 fallback exemplar is Appendix A5 (Bombe Bensa, Ethiopia · Natural) and supersedes §2(b)'s "washed" wording. Real bean, real notes.
- D6 (U9): ALL_STEPS derived in onboardingConstants (not imported from OnboardingFlow) to avoid a circular import; verified byte-identical to STEPS.
- D7 (U8): Appendix A1 acknowledgment map keyed by R02's stored goal values (aiden/v60/aeropress/french_press/espresso/all), mapped 1:1 in list order. APPROVED.
- D8 (U3/U8): motion-law clarification. Onboarding mounts in-tree (Gate 5), NOT in a portal, so existing framer listContainer/listItem top-level entrances are safe and stay (sibling convention); CSS keyframes are used for precise per-item staggers (R04 rows, R09 axis beats). The portal ban applies to portaled overlays.
- D9 (orchestrator): R03 fallback acknowledgment em dash replaced with a period (copy law); U3's AXIS_LABELS approved as non-duplicate of the chart's short legend; R11 FEATURES-trim interpretation approved; PROCESSING_MS removed (grep-verified unused).
- D10 (U5 design): scanned bean is held as answers.pendingScanBean (compact fields only), persisted with onboarding state; finish() writes it inside onboardingAnswers (additive). App.jsx's existing one-shot postCompleteAction consumer creates the bean via addBean when action='scan' AND pendingScanBean exists (then clears both atomically) — exactly-once after profile commit, through the existing bean pipeline, no new Firestore plumbing in onboarding. R13b invite affordance approved (loop.yaml R7 wording wins over spec §2 R13b silence): small text button reusing R13's inline redemption component.
- D11 (U5): radius.xl (24) used for the R10 result card per R05 precedent (theme has no 20 token); RuphusThinking captions passed without trailing ellipsis (component appends its own); empty-name scans route to the approved fallback instead of invented placeholder copy. ALL APPROVED.
- D12 (U6): native RC sheet trigger moved from auto-open-on-ready to the "Start my 7-day free trial" CTA tap — the direct consequence of the spec's page-one-recap-before-sheet framing. Close X routes through the existing dismiss/purchase disambig. APPROVED.
- D13 (U7): entitlement verified pre-celebration by polling hasPro/hasUltra via refs (150ms, ≤3s cap); pain keys mapped 1:1 by position to A4's five rows; redemption never touches postCompleteAction (held scan survives). APPROVED.
- D14 (orchestrator): codex P1 finding 2 completed post-Phase-3 — R05_LAST_SWIPE_HOLD_MS + R08_DENIED_DWELL_MS centralized; R08 denied-message em dash fixed (D9 precedent). Codex P1 finding on R09 1500ms was stale (Phase 2 replaced the quip interval).
- D15 (codex P23): spec revisions — A6 error map rewritten to the REAL api/redeem-code.js reason codes (expired doesn't exist client-side); A2 preamble corrected: ordered sign rules are the archetype contract (codex finding 5 resolved as spec-doc fix, not code); R04 NoteBubble line recorded in A6 for traceability (finding 12 resolved as documentation).
- D16 (codex P23 finding 1, blocker): App.jsx held-bean commit reordered to AT-MOST-ONCE — clear flags first (await), then addBean; clear-failure skips creation; addBean failure logs and never retries. A crash can lose the held bean but can never duplicate it.
- D17 (codex P23 finding 6): R09 chart-at-first-beat + staggered axis rows is the APPROVED U3-brief fallback (shared OnboardingPalateChart must not be modified); the axis-row beats carry the assembly narrative.
- D18 (codex P23 finding 7): R10 fallback-after-failed-scan needs no Skip button — its "Got it" CTA IS the continue path (no dead end); spec variant (b) has no skip by design. No retry-scan affordance added (out of copy-bank scope; revisit only if Tal asks).
- D19 (codex P23 findings 2,3,4,8,9,10,11): fixed in code — entitlement-unconfirmed redemptions finish WITHOUT the celebration beat (no false success); R13b busyRef guard kills the Maybe-later/redemption double-finish race; RedemptionInline error map on real codes; R13b content column scrollable; R11 CTA reads Continue once a scan is held; R13 recap adopts R12's left-aligned column; ALL user-facing em dashes in the onboarding tree replaced.
- D20 (codex re-verify rev2): all four fixed — R13b maybe-later catch resets busyRef (4/4 setBusy(false) sites paired); App.jsx gains a beans name+roaster dedup belt against cross-instance/second-device races; R13 price caption joins the left-aligned column; final three user-facing em dashes removed (OnboardingFlow timeout error, R01, R02).
- D21 (codex rv3): dedup belt reads beansRef.current (assigned every render) so the async consumer never checks a stale-empty beans capture. Findings 1/3/4 PASS.
- D22 (U11): harness decisions APPROVED — narrow console-error exception for R10's deterministic scan-failure log (scenarios 6b/6c only); r6→r5 back-nav equivalence for the wipe guard (R09 hides Back by design); granted-camera seeded via the app's own localStorage resume (non-native platforms always resolve 'unavailable'); per-scenario isolated WebKit contexts. verify-onboarding.mjs = 14 scenarios, ~77s, 3x stable.
