# U6 run projections

This directory intentionally contains no real-provider results. A paid run may
add only checksum-verified sanitized JSONL projections after the U1 identity,
retention, budget, U4, and U5 gates pass. Synthetic tests generate projections
in memory and must not be committed as real-model evidence.
