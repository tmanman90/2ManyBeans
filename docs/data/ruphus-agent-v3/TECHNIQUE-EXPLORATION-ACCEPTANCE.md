# Technique exploration acceptance

Status: implementation in progress; no acceptance PASS yet.

Authority: `docs/plans/2026-09-10-001-feat-ruphus-technique-exploration-plan.md`, U1–U6/R1–R9. User requested Luna max workers with root orchestration. The verbatim MSW Kernel governs admitted work. Separate worker worktrees preserve the existing Dev checkout and unrelated native plist.

## Required journeys

| Journey | Required observation | Evidence |
|---|---|---|
| First technique request | One named response and one compact card without another user message | Native V60, Kalita155 and185 same-turn cards passed; Switch remains failing |
| Another / compare / first again | Distinct eligible family; comparison creates no duplicate; historical selection does not reactivate stale action | Native Kalita another/new-chat reset passed; comparison/history have deterministic and rendered evidence, native sequence remains open |
| Kalita155 and185 | Source-faithful named options; no implicit size change | Native155 scale/Save/Undo and185 source-unit/timer-start/leave passed; exact-size rejection covered deterministically |
| Switch03 | Hybrid and immersion remain distinct; valve instructions and phase capacity survive preview and execution | Source/runtime/timer checks passed; fresh native replay still fails tool_round_limit on8b2cf15 |
| Dose → Start → return → Save → Undo | Same validated snapshot throughout; final saved recipe restored | Native V60 trial/later-Save/Undo and Kalita155 scale/Save/Undo passed; injected Switch source journey passed; native Switch open |
| Missing / unavailable / reference / exhausted | Useful truthful response, no fabricated timer or Aiden substitution | Deterministic coverage exists; native reference/exhaustion remains open |
| Retry / cancellation / relaunch | Stable identities, no duplicate mutations, login and cards retained | Native signed-in relaunch restored source trial/cards; cancellation/idempotency covered deterministically; native interruption sequence open |
| Rendering | Composer does not cover controls; small viewport, large text, reduced motion, return focus | Settled320px/measured125% text/reduced-motion browser proof passed; native V60/Kalita cards and controls reachable above composer |

## Evidence boundaries

- Source fidelity: root independently ran `node --test scripts/ruphus-technique-source-fidelity.test.mjs` at integrated source commit `1ee5bfa`: 8/8 pass. This proves the port's scoped fixtures, not selectable runtime coverage or completed U2.
- Same-turn runtime: root ran the conversation, U2 runtime, endpoint and conversation-contract suites at `ba94722`: 83/83 pass, including structured-only selection, negated/comparison prose, cancellation and domain recovery.
- Rendered browser: `node scripts/verify-ruphus-recipe-first-ui.mjs` passed. Existing preview dose/start/no-save flow and one-request runtime-frame-to-card rendering passed with no writes or page errors. Root visually inspected `/tmp/ruphus-same-turn-technique-mobile.png`; the named card and View recipe control are readable. This fixture proves actual runtime output rendered by the card component, not full ChatTab, live provider, or native acceptance. Verifier lint passed; JSX fixture is not covered by the repository ESLint configuration and was compiled by the rendered harness.
- Source timer core: root independently ran `scripts/ruphus-source-timer.test.mjs` after `cf64f0c`: 6/6 pass. UI and durable outbox integration remain pending.
- Integrated local milestone (2026-09-10): source timer UI `524a88f`, backend `52d7c20`, historical continuity `9ad673e` and requested-dose correction `406f522` are integrated. Root's focused source backend/timer/UI/continuity command passes 21 tests; browser source timer verifier passes with fake Firebase configuration and all non-local/non-GET requests blocked. Root inspected `/tmp/ruphus-source-timer-mobile.png`. `npm run build` passes with existing dynamic-import/Browserslist warnings. The extended recipe-first verifier also renders the actual read-only historical inspector without preview regeneration or actions. Native, live-provider, complete-attempt persistence and complete conversational entry into new Kalita/Switch routes still require integration/acceptance; this is not product PASS.
- Live provider: one signed-in native request executed against candidate 422028f, costing $0.003843. It returned a named Hoffmann recommendation but no artifact, so R1 failed for that request. Last settled authoritative total: $48.039648, reserved $0, cumulative authorization $55. Re-read before paid dispatch; copied ledgers are not authoritative.
- Final route integration: `1c91a60` connects exact legacy Kalita155/185 and Switch03 recipes to source options through the provider schema, gates new schedules on the advertised client capability, and supports current-base validated historical reuse. Root ran 50 combined runtime/conversation/endpoint/Kalita/Switch/source-completion tests: all passed. The only initial failure was an obsolete assertion that Switch03 must remain discussion-only; it now asserts source-backed Switch03 options rather than standard V60 substitutions. Both rendered verifiers passed after integration; targeted production/test ESLint and the build passed. These remain local proofs, not live or native acceptance.
- Source action journeys: worker commit `b3c4695` adds two complete injected Kalita155/Switch03 journeys through the real preview handler, proposal repository and command service: dose edit, immutable Brew once snapshot, completion, later promotion, Undo, duplicate recovery and stale/owner rejection. Worker reports 2/2 journey tests plus focused regressions and lint passing. This is deterministic API evidence, not native or live provider evidence.
- Root subsequently read and ran `node --test scripts/ruphus-source-journey.test.mjs` at 3badbc6: 2/2 passed. The suite checks reconstructed stored documents, native g/mL quantities, immutable reviewed snapshots, one attempt/revision per repeated command, later promotion and exact original snapshot restoration. Its database is injected; it does not prove Firestore emulator execution, actual timer rendering or native navigation. Those portions of TE04/TE07 remain open.
- Coherent Dev candidate: preview `https://twomanybeans-ruphus-oqrw05np8-tmanman90s-projects.vercel.app` is READY in the isolated Dev Vercel project, tagged product commit `422028f81faf0c2017c8768726ee140f64aa44a5`. Later `b3c4695` is test-only. The isolated worktree lacked ignored native project files; the existing recipe-first native project was copied without its old web assets, DerivedData or user data, then the guarded Dev configuration and new web assets were applied. XcodeBuildMCP simulator compile succeeded in 58.5s (dependency warnings only). Built app is `com.talmeltzer.coffeehub.dev`, `2manybeans Dev`, native 1.0(41), with the isolated Firebase web configuration and matching Dev Google client, updater disabled and channel dev. Built JS fingerprint: `d511986b99ddebd97691d20fa9383a7d5061e548448ef72c359502b2971ddfb5`. No install/launch of this candidate yet; shared simulator was voluntarily released for GETUP's final gates.
- Native preservation preflight: read-only isolated operator lookup found the unique Google account (display name `T M`) separately from the providerless seeded account. Current owner recipe baseline is 57 saved recipe snapshots, held as hashes by the temporary operator companion; no recipe writes or authentication injection occurred. This current baseline supersedes historical counts, not their evidence. No new paid dispatch/reservation has occurred.
- Native preflight (2026-09-10): GETUP explicitly released shared iPhone17Pro simulator `7EC6BF90-33B7-4B1A-A651-464B4AC9AA9E`. XcodeBuildMCP launched existing `com.talmeltzer.coffeehub.dev` successfully (PID5700). Direct Simulator screenshots show Tal's signed-in profile, Fellow Ode Gen2 setting and populated Rotation including Jar1 Columbia Risaralda Milan. No install, erase, logout, account/data mutation or model request occurred. XcodeBuildMCP semantic snapshot fails with the known CoreSimulator architecture-load error; direct Computer Use screenshots/clicks work. Existing-app preflight is not acceptance of the new build.
- Phone, production and physical brew: not touched; not prerequisites for the authorized simulator journeys.
- Personal sensory/unscripted verdict: pending and separate from engineering.

## Preservation — historical pause, superseded by resumed acceptance below

The operator companion compared all 57 original/current saved recipe hashes after the first native conversation, reporting `restored:true` and no changed slots. No recipe mutation journey has yet run. GETUP released the shared simulator; Coffee installed the candidate in place and preserved the signed-in account. Native interaction is now paused at Tal's request because Simulator captured the Mac keyboard. The mirror helper was stopped and the Simulator viewer quit; `pgrep -x Simulator` confirmed it absent. Do not reopen either foreground Simulator or iPhone Mirroring while Tal is working.

### Installed candidate and live finding

Candidate 422028f was installed and launched in place on the canonical signed-in Dev simulator. Built and installed public JavaScript assets match SHA-256 `59ad8de2e6a42a0edece6e630c89a539c6ee111e2b2dbcaeefd167ee439635fe` (sorted `public/assets/*.js` contents concatenated); the earlier fingerprint above used a different calculation. Bundle display name, Dev bundle ID, preview backend, isolated Firebase project, Ask Professor Ruphus label, updater disabled and dev channel were checked. This supersedes the earlier compile-only/no-install milestone, not the remaining journey gates.

The exact live request was `What is an interesting different technique for jar one with the v60?`. Production readers returned technique options and a recipe; the persisted reply recommended Hoffmann but contained no artifacts. Expanded `What is` wording was classified as information-only while contracted `What's` was accepted. Commit `8e6b05f` adds a narrow recommendation-question exception while retaining information-only exclusions. Root's conversation/Kalita/Switch command passes 18/18, including actual runtime artifact output for this prompt; targeted source and test lint pass. The fix is local only: the installed candidate and preview still point to 422028f, so live closure remains unproven.

Background verification after pausing native input: root's build at 8e6b05f passes (2424 modules, existing dynamic-import/Browserslist warnings), and the corrected headless rendered verifier passes. No new paid call occurred; the original ledger still reports $48.039648 spent and $0 reserved. A fresh operator preservation read returned HTTP401, so it is not new preservation evidence; the last successful 57-snapshot comparison above remains the recorded check. Refresh supported operator authorization before the next cloud read, without touching the user's simulator login.

### Corrected private candidate (not installed)

After the background verification, preview `https://twomanybeans-ruphus-6lgwnteqe-tmanman90s-projects.vercel.app` reached READY as deployment `dpl_81hK5rKYa63YsrqSNMhBm5VDE1wq`, in isolated project `twomanybeans-ruphus-dev`, with source commit `0bfea57f9e5d65b4ba3405e795fc89ebfb4198e6` (includes 8e6b05f). Root independently checked project, non-production target and source metadata. Unauthenticated POSTs to agent, preview and command endpoints each return HTTP401 JSON. This verifies deployment/auth boundaries, not successful authenticated conversation.

Matching client assets were rebuilt using managed preview configuration in memory, then XcodeBuildMCP compile-only succeeded in 22.452s with no reported warnings/errors. Built native app remains `2manybeans Dev`, `com.talmeltzer.coffeehub.dev`, 1.0(41); expected preview URL, isolated Firebase project, Ask Professor Ruphus label and source capability are present, updater disabled, channel dev. Sorted public JavaScript content SHA-256 is `7e9369c3c60347bf67edac0e8ebf4e99ed614715bf28f7c777960ea0f218abda`. Reused the existing DerivedData directory; did not create another simulator or native build tree. **Not installed or launched**: current signed-in simulator installation is still the earlier 422028f candidate. `pgrep -x Simulator` confirms the desktop viewer remains absent.

Supported Firebase CLI operator refresh succeeded without changing app authentication. The original in-memory baseline then compared successfully: 57 original/current saved recipes, `restored:true`, no changed slots. No paid model request, Capgo upload, phone interaction or production change occurred in this candidate preparation.

### Mac non-interference constraint

A bounded read-only worker audit did not establish a supported, focus-independent text-input path. XcodeBuildMCP headless launch suppresses desktop opening but its keyboard helpers reject headless operation; non-headless helpers may foreground the desktop UI. serve-sim's direct touch path works in the observed mirror, but the earlier text-input attempts did not enter text. Its startup launches Simulator in the background, and the Xcode27 keyboard bridge requires a visible/focused Device Hub. Source comments about iOS27 do not by themselves explain this iOS26.4 guest. Do not infer that an untested keyboard fallback works, or reopen the frontend to obtain proof while Tal is using the Mac. The investigation made no native calls or state changes. Native acceptance remains open until a non-interfering path is verified or Tal permits foreground simulator testing again; no phone is required.

### Resumed native acceptance — September 10, evening

Tal explicitly said `resume simulator`, lifting the foreground-testing pause. The corrected private candidate above was installed in place and launched on the canonical iOS26.4 simulator through XcodeBuildMCP. Installed bundle verification matches `7e9369c3c60347bf67edac0e8ebf4e99ed614715bf28f7c777960ea0f218abda`, the corrected private preview, isolated Firebase, Dev bundle/display name, updater disabled and dev channel. This supersedes the earlier **not installed** status. No uninstall, erase, sign-out or phone interaction occurred.

Initially all simulator input, including Home, was ignored. A normal Simulator Device > Restart (not erase) restored touch navigation. The exact simulator's hardware-keyboard connection was then disconnected through its Keyboard menu; the iOS software keyboard and accessible key buttons appeared. `Send Keyboard Input to Device` was not enabled. The temporary browser mirror was stopped. On-screen key taps successfully entered and sent the exact prior failing request: `What is an interesting different technique for jar one with the v60?`.

The signed-in native reply now displayed a named Tetsu Kasuya 4:6 recipe card in the same turn, with ratio 1:15 and View recipe, fully visible above the composer. View opened the recipe preview rather than a timer. Its 20g/300g preview changed to 21g/315g at the same 1:15 ratio after one plus tap; the preparation dose and first pour also scaled. This is observed native evidence for this V60 turn and preview, not completion of the eight journeys or Kalita/Switch native acceptance. The operator's terminal usage reconciliation recorded one request, no failure code, cost $0.001596, cumulative settled $48.041244 under the $55 authorization. Before the request the original 57 saved-recipe hashes still matched exactly. No Save/Brew once mutation has occurred in this resumed session.

The following native `Show me another one` turn failed TE01: it named Hoffmann but explicitly said no full review card could be prepared. Read-only terminal telemetry records `read_technique_options`, then `read_recipe`, with no proposal tool dispatch and no endpoint failure. Cost $0.003564; settled total $48.044808, reserved $0. Native input and transport were working. The lexical technique gate requires the literal word `technique` or `method`, so this contextual follow-up currently fails readiness despite an earlier delivered technique card. A context-bound regression/fix is under investigation; do not loosen owner/source/action boundaries or count this journey as passed. After returning from the first preview, its card remained visible and all 57 saved recipe hashes still matched the original baseline.

An independent explicit request, `Try a different kalita technique for jar one`, reached the app's `physical_grind_required` recovery instead of a source card. The owner setup is Fellow Ode Gen2; existing deterministic Kalita contexts omitted `setup.grinder`, leaving the generic Ode validation unexercised for source-native grind descriptions. Source grind provenance must remain distinct from numeric app/Ode settings; the corrective boundary and regression are open. This terminal request cost $0.001765; cumulative settled spend is $48.046573 with no outstanding reservation. No saved recipe mutation occurred.

### Visual-proof correction

### Native-failure remediation at 20fef49

The exact two native failures above are fixed locally: target-bound delivered-card state now admits `Show me another one` without making generic comparisons/new chats actionable; native source grind descriptors bypass numeric Ode conversion while ordinary Ode proposals retain physical-click validation. Failed proposals do not consume a technique family. Root inspected the four-file diff and independently ran conversation/Kalita/Switch tests: 21/21 passed, targeted production ESLint and diff-check passed. The Dev simulator remains signed in; all 57 original saved recipe hashes match.

Deployment `dpl_6KDtoKSaZ4WA2wVdn4eCcfe9CxAi`, preview `https://twomanybeans-ruphus-nv6y3l411-tmanman90s-projects.vercel.app`, is READY, non-production, belongs to isolated Dev project and is pinned to `20fef49330960ab8559cba683de6d6f358569672`. Native build-and-run succeeded in 20.4 seconds. Built and installed JS-content fingerprints match `ad689e13ee338749a9641a2f038a866ec4eb3c5c0e40ef837a595025e0e6a2f5`; both verify Dev bundle/display name, corrected preview, isolated Firebase, Ask Professor Ruphus label, autoUpdate false and dev channel. Signed-in Rotation and chat persisted after the in-place install. These installation results alone do not close TE01/TE02.

Native replays now delivered Kurasu Wave 155 for `Try a different kalita technique for jar one` and a distinct Foundation Coffee Roasters Wave 155 card for `Show me another one`. Costs $0.001736 and $0.001973, settled cumulative $48.050282, reservation cleared. Both View controls were visible above the composer. View exposed a separate `source-ratio-adaptation-unsupported` toast: ChatTab passed the source's unchanged ratio as an explicit adaptation. Commit 40962c4 omits that redundant ratio argument for opening/dose edits, uses canonical source dose bounds instead of stale 20g-only bounds, and simplifies technique handoff copy. Root's affected preview/source journey/runtime/Kalita/Switch suite passed 25/25, ESLint/diff-check passed. Native preview retest remains pending. SourceProjectionPreview also lacks dose editing and Save controls present in the generic branch; U5 remains open until those native controls and their journey are proven.

Commit `1c0ca4e` added 320px/reduced-motion coverage, but root inspection found unsettled opacity and no proof that Chromium enlarged text. Correction `cf04064` waits for settled modal opacity/translation and measures baseline versus enlarged glyph/line-box sizes. Chromium ignored 200% text-size-adjust, so the harness explicitly applies a 125% text-only fallback (not page zoom). Root reran the verifier at 8e6b05f: card title 17→21.25px, preview title 24→30px, historical title 17→21.25px; document width remains 320px, View control 46px, focus return passes, zero writes/errors/blocked requests. Root inspected the corrected preview screenshot. This closes the browser proof gap; it is not native Dynamic Type or composer acceptance.

Use supported existing signed-in Dev seams; no authentication backdoor or logout. Capture original saved recipe identities before test actions and confirm Undo/readback afterward. Preserve all historical diagnostic ledgers and reports. No production, Firebase-rule, Capgo-channel or external hardware changes are authorized by implementation alone.

### Native trial recovery and source controls

On installed 20fef49, a V60 request after the Kalita conversation correctly delivered James Hoffmann One-Cup V60. View showed 20g/333g; explicit Start opened that technique's timer. Root paused and stopped the test timer (no physical brew/tasting claimed), returned through Rotation to Chat, and observed the durable Brew once receipt. All 57 saved recipe snapshots were unchanged. Asking `Can I save that trial permanently` recovered the exact hot-V60 trial with an explicit Make this my recipe control. Clicking it changed only the tested v60_hot slot; its Recipe updated receipt exposed Undo. Clicking Undo restored all 57 original snapshots, independently read back by the preservation helper. Recovery turn cost $0.001556; cumulative settled spending is $48.053732, no outstanding reservation. This closes the observed trial/save/undo path, not all source or recovery cases.

Commit 23aede7 adds source-preview dose controls, native grind/quantity display, and separate guarded Save/Start controls. Root inspected the diff and independently reran the rendered recipe-first verifier and production ESLint/diff-check. The rendered source dose changed 24g→23g with 360mL→345mL and 50g→47.92g; supported original 36g/440mL remained actionable. These are rendered local results; the combined native candidate is being prepared for retest.

The combined 23aede708fd492d67902cb346fd39665ec8a0677 candidate is now installed in place. Preview `https://twomanybeans-ruphus-dc2ymvy77-tmanman90s-projects.vercel.app` (deployment `dpl_9JmffG8kqEUaES9zS4s8RfaWvHUp`) was independently verified READY, isolated Dev project, non-production target and matching source metadata. XcodeBuildMCP build-and-run succeeded in 20.7s, PID45951. Built/installed JS-content hashes match `1ee550669021592dabaf8bf5cef44d35e1228e582a5070e7c552f3bbe5c73ddc`; both verify the new preview, isolated Firebase, Dev bundle/name, Ask label, autoUpdate false and dev channel. Login persisted, and Chat restored the undone trial/receipt after relaunch. No production/Capgo/phone changes.

On the combined native candidate, New chat reset exploration: the fresh Kalita request returned Kurasu again in the same turn. View opened without the prior ratio-adaptation error. Native plus changed 13g/185.71g to original 14g/200g and native stage quantities to 30g/60g/200g. Coarse source grind and 92°C remained visible. Separate Start/Save controls were reached by mirror scrolling. Save changed only the tested kalita_hot slot; an independent inventory read confirmed 14g/155 Wave. Undo restored all 57 original recipe snapshots. Request cost $0.0018, cumulative $48.055532, no outstanding reservation at closure. This proves native source preview/scale/save/undo for Kalita155; 185 and Switch journeys remain separate.

Native `Try a different hot kalita 185 technique for jar two` returned Onyx Monarch Wave185 in the same turn for Dehong Yuan Yi Yuan, 20g/320g. View retained size185, ~600µm source grind and 200°F. Explicit Start opened a source guide at 0:00 with the correct 20g/320g and 40g first-stage quantity; the clock remained unstarted until Start first pour. It then advanced while the stage remained in progress awaiting manual Pour finished confirmation. Root did not confirm physical pouring; Leave and resume later returned to the immutable trial recipe. All 57 saved recipe snapshots still matched. This is native source-clock/quantity and leave behavior, not physical brew confirmation. The turn cost $0.001583.

Switch native failure is confirmed, not an absent-data assumption. Read-only Dev inspection found one FINISHED El Vergel with handBrewRecipes.v60 `{variant:'switch',v60Size:'03',device:'v60',coffeeGrams:15,method:'pour-over'}`. Chat nevertheless claimed only a hot V60 existed, asked equipment and then mode, and refused a Switch alternative after explicit ribbed glass03/matchingpaper/Hot clarification. Costs for those three turns were $0.001419/$0.001856/$0.001765. The context lookup is under targeted remediation; no Switch native PASS. Cumulative spending after these and the185 turn is $48.062155, reservation cleared.

Switch remediation 5fe6a2f preserves allowlisted recipe equipment fields in public evidence and the exact current configuration in technique options. Finished coffees stay outside the public rotation snapshot, but their owner-resolved saved Switch identity now survives the tool projection. Root inspected the three-file change and independently passed runtime/evidence/Switch23/23 plus source ESLint/diff-check. Removed a proposed new four-record truncation rather than inventing a cap. Raw recipe records remain excluded. Native replay is pending; no closure claim yet.

The 5fe6a2f Switch correction is installed in place. Preview `https://twomanybeans-ruphus-1x0ww2g2r-tmanman90s-projects.vercel.app` / `dpl_6WeFkaSXaTFVEjXJ7xexMj3gtjJk` verified READY, correct isolated project/non-production/source commit. XcodeBuildMCP build-and-run succeeded23.7s, PID3601. Built/installed JS hash `663eea91b0f605bd4add372e97cfa553610e36c0a5e04c46973852e1c340d60b` matches, as do Dev bundle/display name, preview, Firebase, label, autoUpdate false and channeldev. Existing signed-in source trial recovered to the immutable recipe after install; no completed physical step was inferred. Switch live replay remains separate.

### Switch round-limit correction and current native result

8b2cf1558596bd889f53f646be9139ff88a1e8d5 adds bounded recovery for redundant reads after trusted technique options, retaining current call IDs and exact-target proposal checks. Root inspected the change and passed26/26 focused runtime/provider/Switch tests plus targeted lint/diff-check. This deterministic result did not close the native failure.

Preview `https://twomanybeans-ruphus-kxzoiqwyn-tmanman90s-projects.vercel.app`, deployment `dpl_6iz7cxXKyNUdkfonhae7WMjpfrde`, was verified READY against isolated Dev project and exact source SHA. The existing guarded asset helper checked non-production target and source identity. XcodeBuildMCP build-and-run succeeded20.9s, PID32870. Built/installed JS-content fingerprints match `bffaf7a37ce8cf6c89dba8109c33cf4eee7d4eb5df9e53c8446d99b5da9b32e5`; Dev bundle/name, preview, Firebase, Ask label, updater disabled and dev channel all passed. Login and in-progress source trial survived the in-place install.

Fresh New chat request `Try a different switch technique for el vergel` still returned the generic AI error. Terminal Dev telemetry reports `tool_round_limit`, with dispatched tools `read_coffee_evidence` and `read_technique_options`. The rejected next provider-call arguments are not in the retained telemetry, so no exact malformed-argument cause is claimed. Corrective work remains restricted to this failing boundary. Request cost $0.001665; cumulative ledger $48.065408, reserved0. Before this request all57 original saved recipe hashes matched. No production, phone, Capgo or authentication changes occurred.

Native TE05 on8b2cf15: `Show me an iced switch recipe` correctly explained that the saved Switch is hot and the saved iced recipe is Kalita, without minting an executable card or reporting connectivity failure. The follow-up `What about the two temperature devil recipe` also produced no card/error, but only asked for the recipe because it was not saved. This is not full reference-guidance PASS: the existing researched God/Devil entry in `v60SwitchSourceRegistry.js` was not surfaced. Costs $0.001764 and $0.002758, cumulative $48.069930, reserved0. After both requests, all57 original/current saved recipe hashes match exactly. A separate zero-paid, read-only production-tool probe with the actual saved El Vergel recipe confirms both Switch03 alternatives are executable at15g and readiness is true; source availability is not the observed chat failure.

4c4a6533a6e9f300069ef4d72cc0d097a9ac5299 closes a demonstrated deterministic guard gap: mixed read/wrong-target proposal requests are rejected before dispatch, then the existing bounded proposal-only continuation receives the exact trusted target/options. Repeated invalid calls remain non-actionable. R2 handoffs now include stage-derived structural differences (rather than author/dose alone), deduplicated only against the exact sentence. Root inspected all changed production paths and independently passed35/35 runtime/Switch/Kalita/conversation tests, source ESLint and diff-check. The verbatim MSW Kernel was propagated to the Luna execution worker. No native closure is inferred from those tests; candidate deployment/replay follows separately.

Candidate4c4a653 is installed in place after guarded asset preparation. Preview `https://twomanybeans-ruphus-f1soz3feu-tmanman90s-projects.vercel.app`, deployment `dpl_4qh4SRsnLhn66kNuBkvHbdc8nwLf`, verified READY, non-production, exact source SHA and isolated project. XcodeBuildMCP build-and-run succeeded20.8s, PID55515. Built/installed JS hash `093440e76a6ea367db9626dadd373476ac55a93e6fe9afda97557ff25582e05e` matches, with all Dev identity/preview/Firebase/label/updater/channel checks passing. Signed-in Rotation and the prior immutable source trial were retained. The fresh Switch replay is recorded separately from this install proof.

Fresh native replay on4c4a653 still failed `tool_round_limit`; terminal cost $0.001601, cumulative $48.071531, reserved0. The earlier guard correction alone is not native closure. Root then reproduced the underlying method-binding gap with the actual owner's recipe shapes, without a provider call: El Vergel has `[aiden,v60_hot,kalita_hot,kalita_iced]`; `resolveMethod` for the exact Switch request returns Aiden/M4 because the method grammar does not recognize Switch. Composite evidence can therefore bind a different slot before Switch options arrive. The isolated prior runtime fixture had only a V60 recipe and did not cover this input. R3 correction now requires multi-recipe explicit-Switch priority, verb/equipment disambiguation, and same-slot wrong-variant refusal. A current Switch test incorrectly expects generic V60 options for an explicit Switch request against a standard-V60 base; that assertion must be corrected, not counted as coverage.

## Source discovery affecting acceptance

### Explicit app dose guide policy

`ruphus-manual-source-checkpoint-v2` preserves the requested serving; option discovery never clamps it to 20g. Kalita adaptations use existing exact-size `KALITA_CONFIGURATION` dose bounds (155:12–20g;185:15–36g). These are app equipment policy, not author-endorsed timing or sensory claims. Typed water quantities scale proportionally; published checkpoints and pour durations are retained as an explicitly disclosed app guide. Original source prose remains original rather than being rewritten.

Switch adaptations are downscale-only: the existing 15g equipment-policy floor applies, and the upper bound is the smaller of the source's original load and existing 30g app dose bound. Every retained-water phase and the grounds load therefore remains no greater than the admitted original; no inferred 340g/360mL raw-input cap is used. The supported original source recipe remains available unchanged (including HARIO's 36g/440mL original). Outside-policy requests remain readable references with an explanation, never silently changed or promoted to timer-ready. Focused regression proves native units, unchanged phase durations, original source doses, 20g adaptation and just-outside boundaries.

On 2026-09-10, root retrieved and visually inspected both pages of HARIO's official [SSD-360 manual](https://www.hario.com/product/SSD-360.pdf), printed revision `80682101 SSD-360説明書2301`. It specifies the 03 paper filter, 36g coffee with approximately 440mL hot water, closed-valve pouring followed by approximately two minutes of steeping, then release. Numeric brew temperature is not specified. This provides a primary-source full-immersion candidate; it is not implementation or brew evidence.

The manual labels 360mL as **finished capacity**. Do not reinterpret that number as a measured bowl brim/input limit. Capacity validation must preserve the supported original while evaluating retained water, coffee load and any explicit scaling policy; it must not silently clamp the recipe. The older `manual_pdf/SSD.pdf` and USA-linked `product/SSD-200.pdf` describe the 02 and do not by themselves prove an original 03 recipe.

Journey definitions: `scripts/fixtures/ruphus-techniques/conversation-cases.json` (eight cases). JSON shape and unique IDs checked; these definitions are not executed acceptance evidence.
