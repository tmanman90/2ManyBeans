# Technique exploration acceptance

Status: implementation in progress; no acceptance PASS yet.

Authority: `docs/plans/2026-09-10-001-feat-ruphus-technique-exploration-plan.md`, U1–U6/R1–R9. User requested Luna max workers with root orchestration. The verbatim MSW Kernel governs admitted work. Separate worker worktrees preserve the existing Dev checkout and unrelated native plist.

## Required journeys

| Journey | Required observation | Evidence |
|---|---|---|
| First technique request | One named response and one compact card without another user message | Deterministic runtime and rendered component passed; live/native pending |
| Another / compare / first again | Distinct eligible family; comparison creates no duplicate; historical selection does not reactivate stale action | Pending |
| Kalita155 and185 | Source-faithful named options; no implicit size change | Pending |
| Switch03 | Hybrid and immersion remain distinct; valve instructions and phase capacity survive preview and execution | Pending |
| Dose → Start → return → Save → Undo | Same validated snapshot throughout; final saved recipe restored | Pending |
| Missing / unavailable / reference / exhausted | Useful truthful response, no fabricated timer or Aiden substitution | Pending |
| Retry / cancellation / relaunch | Stable identities, no duplicate mutations, login and cards retained | Pending |
| Rendering | Composer does not cover controls; small viewport, large text, reduced motion, return focus | Pending |

## Evidence boundaries

- Source fidelity: root independently ran `node --test scripts/ruphus-technique-source-fidelity.test.mjs` at integrated source commit `1ee5bfa`: 8/8 pass. This proves the port's scoped fixtures, not selectable runtime coverage or completed U2.
- Same-turn runtime: root ran the conversation, U2 runtime, endpoint and conversation-contract suites at `ba94722`: 83/83 pass, including structured-only selection, negated/comparison prose, cancellation and domain recovery.
- Rendered browser: `node scripts/verify-ruphus-recipe-first-ui.mjs` passed. Existing preview dose/start/no-save flow and one-request runtime-frame-to-card rendering passed with no writes or page errors. Root visually inspected `/tmp/ruphus-same-turn-technique-mobile.png`; the named card and View recipe control are readable. This fixture proves actual runtime output rendered by the card component, not full ChatTab, live provider, or native acceptance. Verifier lint passed; JSX fixture is not covered by the repository ESLint configuration and was compiled by the rendered harness.
- Source timer core: root independently ran `scripts/ruphus-source-timer.test.mjs` after `cf64f0c`: 6/6 pass. UI and durable outbox integration remain pending.
- Integrated local milestone (2026-09-10): source timer UI `524a88f`, backend `52d7c20`, historical continuity `9ad673e` and requested-dose correction `406f522` are integrated. Root's focused source backend/timer/UI/continuity command passes 21 tests; browser source timer verifier passes with fake Firebase configuration and all non-local/non-GET requests blocked. Root inspected `/tmp/ruphus-source-timer-mobile.png`. `npm run build` passes with existing dynamic-import/Browserslist warnings. The extended recipe-first verifier also renders the actual read-only historical inspector without preview regeneration or actions. Native, live-provider, complete-attempt persistence and complete conversational entry into new Kalita/Switch routes still require integration/acceptance; this is not product PASS.
- Live provider: not run. Authoritative original-worktree ledger read at setup: spent $48.035805, reserved $0, cumulative authorization $55. Re-read before paid dispatch; copied ledgers are not authoritative.
- Final route integration: `1c91a60` connects exact legacy Kalita155/185 and Switch03 recipes to source options through the provider schema, gates new schedules on the advertised client capability, and supports current-base validated historical reuse. Root ran 50 combined runtime/conversation/endpoint/Kalita/Switch/source-completion tests: all passed. The only initial failure was an obsolete assertion that Switch03 must remain discussion-only; it now asserts source-backed Switch03 options rather than standard V60 substitutions. Both rendered verifiers passed after integration; targeted production/test ESLint and the build passed. These remain local proofs, not live or native acceptance.
- Native preflight (2026-09-10): GETUP explicitly released shared iPhone17Pro simulator `7EC6BF90-33B7-4B1A-A651-464B4AC9AA9E`. XcodeBuildMCP launched existing `com.talmeltzer.coffeehub.dev` successfully (PID5700). Direct Simulator screenshots show Tal's signed-in profile, Fellow Ode Gen2 setting and populated Rotation including Jar1 Columbia Risaralda Milan. No install, erase, logout, account/data mutation or model request occurred. XcodeBuildMCP semantic snapshot fails with the known CoreSimulator architecture-load error; direct Computer Use screenshots/clicks work. Existing-app preflight is not acceptance of the new build.
- Phone, production and physical brew: not touched; not prerequisites for the authorized simulator journeys.
- Personal sensory/unscripted verdict: pending and separate from engineering.

## Preservation

Use supported existing signed-in Dev seams; no authentication backdoor or logout. Capture original saved recipe identities before test actions and confirm Undo/readback afterward. Preserve all historical diagnostic ledgers and reports. No production, Firebase-rule, Capgo-channel or external hardware changes are authorized by implementation alone.

## Source discovery affecting acceptance

### Explicit app dose guide policy

`ruphus-manual-source-checkpoint-v2` preserves the requested serving; option discovery never clamps it to 20g. Kalita adaptations use existing exact-size `KALITA_CONFIGURATION` dose bounds (155:12–20g;185:15–36g). These are app equipment policy, not author-endorsed timing or sensory claims. Typed water quantities scale proportionally; published checkpoints and pour durations are retained as an explicitly disclosed app guide. Original source prose remains original rather than being rewritten.

Switch adaptations are downscale-only: the existing 15g equipment-policy floor applies, and the upper bound is the smaller of the source's original load and existing 30g app dose bound. Every retained-water phase and the grounds load therefore remains no greater than the admitted original; no inferred 340g/360mL raw-input cap is used. The supported original source recipe remains available unchanged (including HARIO's 36g/440mL original). Outside-policy requests remain readable references with an explanation, never silently changed or promoted to timer-ready. Focused regression proves native units, unchanged phase durations, original source doses, 20g adaptation and just-outside boundaries.

On 2026-09-10, root retrieved and visually inspected both pages of HARIO's official [SSD-360 manual](https://www.hario.com/product/SSD-360.pdf), printed revision `80682101 SSD-360説明書2301`. It specifies the 03 paper filter, 36g coffee with approximately 440mL hot water, closed-valve pouring followed by approximately two minutes of steeping, then release. Numeric brew temperature is not specified. This provides a primary-source full-immersion candidate; it is not implementation or brew evidence.

The manual labels 360mL as **finished capacity**. Do not reinterpret that number as a measured bowl brim/input limit. Capacity validation must preserve the supported original while evaluating retained water, coffee load and any explicit scaling policy; it must not silently clamp the recipe. The older `manual_pdf/SSD.pdf` and USA-linked `product/SSD-200.pdf` describe the 02 and do not by themselves prove an original 03 recipe.

Journey definitions: `scripts/fixtures/ruphus-techniques/conversation-cases.json` (eight cases). JSON shape and unique IDs checked; these definitions are not executed acceptance evidence.
