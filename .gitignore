node_modules
dist
.firebase
.vercel
*.log
.DS_Store

# Environment (catch-all, then whitelist example)
.env*
!.env.example

# AI-generated images (not used by app)
generated_imgs/

# Capacitor native projects (regenerated by `npx cap sync`)
ios/
android/

# Firebase credentials (never commit)
GoogleService-Info*.plist
