rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Each user can read/write their own photos
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Deny everything else
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
